/**
 * Spec alias: homepage hero orbit. Implementation lives in `toy-orbit/`
 * and is dynamically imported from `app/page.tsx` with `ssr: false`.
 */
export { ToyOrbitHero as ToyOrbit } from "@/components/toy-orbit/ToyOrbitHero";
